
~~~~~~~~~~~~~~~~~~ Hostbase project By Koala @ crack-wifi.com @ wifi-libre.com @ kali-linux.fr ~~~~~~~~~~~~~~~~~~~~

 Bienvenido en hostbase

Readme hecho ara los usarios de kali-linux.El script anda bien sobre kali-linux xfce presicamente.

CHANGELOG de esa nueva version:

- Actualizacion de las paginas de phishing para integrar paginas wpa3 que pide de entrar la clave wifi para crear nuevo codigo QR.Los codigos QR van a estar mucho utilizados con el wpa3 en el futuro.
- Actualizacion del tchat que se hace pasar por el servicio technico.
- Anadido de "colorize" que permite de ver la clave wifi en rojo en la consola de hostbase (unicamente para las paginas wpa3).
- Correcion de un "function" que habia olvidado de poner para capturar la clave wifi con el opcion WPS PBC loop.OJO: esa opcion tiene que estar usado independientemente de las otras opciones.Si alguien apoya su boton wps, la clave wifi se pondra en la consola principal de hostbase.
- Anadido de mi e-mail personal para ayudar a la gente o hacer nueva colaboracion.

--------------------------------------------

-1 PARA TODOS DEPENDENCIAS DEL SCRIPT

apt-get install -y build-essential upgrade-system subversion wget g++ iptables pavucontrol ffmpeg sqlite3 libsqlite3-dev libssl-dev libnl-3-dev libnl-genl-3-dev dsniff hostapd isc-dhcp-server pkg-config xterm freeradius apache2 php libapache2-mod-php php-cli tcpdump python3-scapy vokoscreen wireshark bridge-utils devscripts gengetopt autoconf libtool make


____________________________________________



-2 Saber si mi tarjeta wifi esta compatible con hostapd, commando a entrar:
iw list | grep "Supported interface modes" -A 8

Si esta compatible te saldra eso:

Supported interface modes:
		 * IBSS
		 * managed
		 * AP
		 * AP/VLAN
		 * WDS
		 * monitor
		 * mesh point


Si tienes una version de hostapd antigua suprima lo:
apt-get remove hostapd

Y compila lo asi:

wget https://w1.fi/cgit/hostap/snapshot/hostap_2_9.tar.gz
tar -zxf hostap_2_9.tar.gz
cd /root/hostap_2_9/hostapd
cp defconfig .config
nano .config

Decomentar el # en todas las opciones que siguen:

CONFIG_DRIVER_NL80211=y
CONFIG_LIBNL32=y
CONFIG_EAP_PWD=y
CONFIG_WPS=y
CONFIG_WPS_UPNP=y
CONFIG_WPS_NFC=y
CONFIG_RADIUS_SERVER=y
CONFIG_IEEE80211N=y
CONFIG_IEEE80211AC=y
CONFIG_DEBUG_FILE=y
CONFIG_FULL_DYNAMIC_VLAN=y
CONFIG_TLSV11=y
CONFIG_TAXONOMY=y


Y termina con:

sudo make
sudo make install
hostapd -v

________________________________________________

-3 Servidor web apache2: se copia el fichero 000-default.conf que esta puesto con el archivo de hostbase en /etc/apache2/site-available.
Para terminar se copia el fichero apache2.conf que esta puesto con el archivo de hostbase en /etc/apache2

Se copia todo lo que hay en la carpeta paginasAQUI a dentro el répertorio /etc  (son las falsas paginas de phishing)

________________________________________________


-4 Installar las dependencias de los gems de ruby:


apt-get install ruby
apt-get install ruby-dev
gem install highline
gem install rake
gem install bundler
gem install colorize
apt-get install libgtk2.0-dev
apt install gobject-introspection
apt install ruby-gtk2



_________________________________________________
mdk4
apt-get install mdk4

_________________________________________________
berate_ap
apt install berate-ap

_________________________________________________

-5 EN/ES/FR start:

El script se tiene que ejecutar en la carpeta /tmp
Copia y pega la carpeta hostbase-1.5ES a dentro la carpeta /tmp luego entra eso en una consola:
cd /tmp/hostbase-1.5ES
 Y inicia lo asi //
ruby hostbase.rb

--> OJO: no olvidas de empezar por el scan de redes para apagar network-manager <-- 


____________________________________________________


-6 Cuando se entra el nombre de la pagina de phishing se elige al mismo tiempo el typo de ataque a usar, debes éligir las paginas con el boton wps si la red esta en wpa2:
orangewps 
o
movistarwps
o
vodafonewps
o
onowps
o
jazztelwps
o
vodafonewps
etc etc...

La clave esta registrado por defecto en /etc/wpa_supplicant.conf, despuès del attaque para ver si la clave ha sido capturada con el WPS-PBC entra eso en un nuevo terminal:
cat /etc/wpa_supplicant.conf

(Una vez el boton wps apoyado, hay que esperar un poco para que se hace la conexion a travès el wps-pbc)


---------> Si la red esta en wpa3, se tiene que usar las paginas de phishing que pide de entrar la clave wifi para crear un "codigo" qr que va a servir a la victima a conectarse en su red cuando ha entrado su clave wifi.Si entra su clave wifi en la pagina de phishing, se vera en rojo en la consola principal de hostbase.Para el wpa3 el nombre de las paginas de phishing a entrar el la interfaz grafica de hostbase tiene que estar asi:
orangewpa3 
o
movistarwpa3
o
vodafonewpa3
o
onowpa3
o
jazztelwpa3
o
vodafonewpa3

Para ver la clave wifi despuès del ataque si el usario lo ha entrao en la pagina de phishing: cat /var/www/msftconnecttest/cle.txt
----------> OJO: la clave wifi aparece en una linea en el fichero cle.txt por ejemplo asi: 1234567890Bc1234567890Bc  o sea aqui hay que tener en cuenta que hay 2 veces la misma clave porque hay 2 entradas en la pagina de phishing, una para la clave wifi y otra para confirmir y verificar que el usario entra dos claves iguales.Si la clave esta asi en el fichero cle.txt: 1234567890Bc1234567890Bc
la clave sera en realidad: 1234567890Bc
____________________________________________________

-7 Hostbase tiene un tchat para hacer se pasar por el servicio technico.Una vez que has empezado el ataque, puedes ir en la pagina del tchat con tu navegador internet:

http://127.0.0.1

Desde esa pagina de phishing ir a bajo y busar "ayuda en linea" y dar le con el mando.

---> Una ves que estas en la pagina del tchat, TIENES QUE ENVIAR EL MENSAJE EN PRIMERO para que anda el tchat con ese ejemplo:
Nombre: Tecnico    mensaje:   Hola soy del servicio technico como puedo ayudar ?

Despuès si el usario se va en "Ayuda en linea" en la pagina de phishing principal, se pondra en el tchat directamente y podras hablar con el.



_____________________________________________________




8- Para mas potentia este script ANDA con 2 tarjeta wifi porqué automatisa un monton de cosas: el AP encryptado con el WPS, la DoS que sigue el Ap etc... ( y anda con 3 tarjetas wifi si quieres el ataque en 5GHz tambien)

A CADA PRUEBAS:
--> Se tiene que empezar por el scan de redes para parar network-manager y tener informacion sobre la red que quieres
--> Se sale del script cada vez con ctrl+c, eso es para dejar todo bien limpio despuès un ataque...
--> La DoS que sigue el AP anda con wash, wash tiene que estar installado tambien (pero no tienes que installar lo si tienes kali-linux esta installado por defecto)

--> A la salida del script con ctrl+c se réeiniciara network-manager o sea si quieres hacer otras pruebas sin pasa por el scan de redes cada vez, tendras que apagar network-manager por no tener problemo, si network-manager esta andando mientras haces pruebas tendras problemos.Se para con:

systemctl stop NetworkManager.service
systemctl disable NetworkManager.service

Y poner lo en marcha:

systemctl enable NetworkManager.service
systemctl start NetworkManager.service


Mi consejo es de séguir pasando por el san de redes entre 2 ataque porqué el AP puede mover de canal (channel hopping).

Preguntas y soporte en mi e-mail:  dubbleyou633@gmx.com     o en:    wifi-libre.com

____________________________________________________

hostbase by Koala
Under GPLv3 license












